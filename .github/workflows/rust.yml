name: Build and Test
on:
  pull_request:
    branches: ["main"]
env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Run sccache-cache only on non-release runs
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars only on non-release runs
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Build ${{ matrix.rust }}
        run: cargo build --verbose

  test-jetstream:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream
        run: cargo test --verbose --all-features -p jetstream

  test-9p:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_9p
        run: cargo test --verbose --all-features -p jetstream_9p

  test-client:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_client
        run: cargo test --verbose --all-features -p jetstream_client

  test-cluster:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_cluster
        run: cargo test --verbose --all-features -p jetstream_cluster

  test-macros:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_macros
        run: cargo test --verbose --all-features -p jetstream_macros

  test-server:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_server
        run: cargo test --verbose --all-features -p jetstream_server

  test-ufs:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_ufs
        run: cargo test --verbose --all-features -p jetstream_ufs

  test-wireformat:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Test jetstream_wireformat
        run: cargo test --verbose --all-features -p jetstream_wireformat

  run-examples:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - "stable"
          - "beta"
          - "nightly"
          - "1.75.0"
          - "1.76.0"
          - "1.77.0"
          - "1.78.0"
          - "1.79.0"
          - "1.80.0"
          - "1.81.0"
          - "1.82.0"
    env:
      SCCACHE_GHA_ENABLED: on
    steps:
      - uses: actions/checkout@v3
      - name: Set up Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          override: true
      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.6
      - name: Set Rust caching env vars
        if: github.event_name != 'release' && github.event_name != 'workflow_dispatch'
        run: |
          echo "SCCACHE_GHA_ENABLED=true" >> $GITHUB_ENV
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
      - name: Run example echo
        run: cargo run --example echo
