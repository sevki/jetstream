[package]
name = "jetstream_rpc"
version = "5.0.0"
edition = "2021"

[dependencies]
async-trait = "0.1.83"
git2 = { version = "0.18.3", optional = true }
jetstream_wireformat = { version = "5.0.0", path = "../jetstream_wireformat" }
okstd = { version = "0.1.10" }
tokio = { version = "1.40.0", features = ["full"] }

[features]
git2 = ["dep:git2"]
